const init = {
    games: ['Minecraft', 'LOL', 'Valorant', 'Stardew Valley'],
};

export default function reducer(state = init, action, args) {
    switch (action) {
        default: 
            return state;
    }
}